// const Dotenv = require('dotenv-webpack')

// @see https://github.com/netlify/netlify-lambda#webpack-configuration
module.exports = {
  // plugins: [new Dotenv()],
  mode: "development", // *todo* out later; to not minify, when we want
}
